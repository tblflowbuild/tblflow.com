---
import Logo from './Logo.astro';
import LangSwitcher from './LangSwitcher.astro';
import { useTranslations } from '@/i18n/ui';
import { localePath } from '@/i18n/utils';
import { APP_URL, GITHUB_URL, type Locale } from '@/config';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);
const year = new Date().getFullYear();

const columns: Array<{ title: string; links: Array<{ label: string; href: string }> }> = [
  {
    title: t('footer.product'),
    links: [
      { label: t('nav.pricing'), href: localePath(locale, 'pricing') },
      { label: t('footer.nav.features'), href: `${localePath(locale)}#features` },
      { label: t('compare.title'), href: `${localePath(locale)}#comparison` },
      { label: t('nav.login'), href: `${APP_URL}/auth/login` },
    ],
  },
  {
    title: t('footer.resources'),
    links: [
      { label: t('nav.blog'), href: localePath(locale, 'blog') },
      { label: t('nav.docs'), href: localePath(locale, 'docs') },
      { label: t('compare.title'), href: localePath(locale, 'compare') },
      { label: 'FAQ', href: `${localePath(locale)}#faq` },
      { label: 'RSS', href: localePath(locale, 'rss.xml') },
    ],
  },
  {
    title: t('footer.company'),
    links: [
      { label: 'GitHub', href: GITHUB_URL },
      { label: t('footer.nav.contact'), href: 'mailto:contact@tblflow.com' },
    ],
  },
];
---

<footer class="mt-24 border-t border-[var(--border)] bg-[var(--bg-subtle)]">
  <div class="mx-auto max-w-6xl px-4 py-14 sm:px-6">
    <div class="grid gap-10 md:grid-cols-[1.4fr_repeat(3,1fr)]">
      <div class="reveal">
        <a href={localePath(locale)} class="flex items-center gap-2.5 font-semibold tracking-tight">
          <Logo size={28} title="TblFlow" />
          <span>TblFlow</span>
        </a>
        <p class="mt-3 max-w-xs text-sm text-[var(--fg-muted)]">{t('footer.tagline')}</p>
        <LangSwitcher locale={locale} class="mt-5" />
      </div>

      {
        columns.map((column, i) => (
          <div class:list={['reveal', `delay-${Math.min(i + 1, 4)}`]}>
            <h2 class="text-xs font-semibold tracking-wide text-[var(--fg)] uppercase">
              {column.title}
            </h2>
            <ul class="mt-3 space-y-2">
              {column.links.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-[var(--fg-muted)] transition-colors hover:text-[var(--fg)]"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <div
      class="reveal mt-12 flex flex-col gap-3 border-t border-[var(--border)] pt-6 text-xs text-[var(--fg-subtle)] sm:flex-row sm:items-center sm:justify-between"
    >
      <p>&copy; {year} TblFlow. {t('footer.rights')}</p>
      <p>{t('footer.stack')}</p>
    </div>
  </div>
</footer>
